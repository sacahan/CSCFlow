services:
  scheduler:
    container_name: scheduler-container
    build:
      context: .
      dockerfile: Dockerfile.scheduler
    environment:
      - DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/cscflow

  flow:
    container_name: flow-container
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/cscflow
      - REDIS_URL=redis://redis:6379
      - REDIS_PASSWORD="%@z%dVBRS2f8io"
      - JWT_SECRET=rzKI!i5rxxOTKO^EC4f&PMgVLq9%zFb1
      - CORS_ORIGINS=http://frontend:3000
      - VITE_CWA_API_KEY=CWA-5C524E5F-F9DC-41D3-B099-C124C05004BC
      - VITE_API_USERNAME=admin
      - VITE_API_PASSWORD=%1Vt4j9Q
    ports:
      - "8000:8000"
